nonoApp
  .factory( 'LoverRegistryService', function(){
      var registry = {};

      registry.userEmail = '';
      registry.userId = '';
      registry.userDate = {};

      return registry;
    });
